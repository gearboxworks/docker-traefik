#!/bin/bash
# {{ .CreationWarning }}
# {{ .CreationInfo }}

# See gearboxworks/gearbox-base for details.
. /build/include-me.sh

c_ok "Gearbox[{{ .Json.name }}]: Starting."

GBEP="$(which ${GEARBOX_ENTRYPOINT})"
if [ "${GBEP}" != "" ]
then
	${GBEP} ${GEARBOX_ARGS}
else
	# Add your service entry here, if no entrypoint defined in the ref container.
	echo ""
fi

